const configConstants = {
  accessTokenValidityInMinutes: 5,
  refreshTokenValidityInDays: 30,

  // For debugging:
  // accessTokenValidityInMinutes: 0.1,
  // refreshTokenValidityInDays: 1 / (24 * 600),
};
export default configConstants;
